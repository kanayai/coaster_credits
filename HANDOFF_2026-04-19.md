# Coaster Credits Handoff (2026-04-19)

## Session Summary
- Production Supabase runtime was initially failing with: "Supabase is not configured in this environment." 
- OAuth then looped to `localhost` on phone after Google sign-in.
- User could sign in after fixes, but profiles/credits visibility issues remained due to owner-id scoping and profile lifecycle actions.
- Data was re-imported from local export files and app is now mostly working, but user reports only ~70+ credits visible (expected higher for Karim).

## What Was Fixed

### 1) Cloud Build / runtime Supabase env injection
- Added Vite env pass-through to Docker build and Cloud Build substitutions:
  - `Dockerfile`
  - `cloudbuild.yaml`
  - `README.md`
- Required deploy substitutions now include:
  - `_VITE_SUPABASE_URL`
  - `_VITE_SUPABASE_ANON_KEY`
  - `_VITE_SUPABASE_REDIRECT_URL`

### 2) OAuth redirect hardening
- Replaced dynamic OAuth redirect (`window.location.origin`) with fixed env var:
  - `VITE_SUPABASE_REDIRECT_URL`
- Files changed:
  - `services/supabaseClient.ts`
  - `context/appLifecycle.ts`
  - `Dockerfile`
  - `cloudbuild.yaml`
  - `README.md`
  - `tests/appLifecycle.sync-qa.test.ts`

### 3) Profile management improvements
- Added profile deletion capability in UI and data layer.
- Fixed cloud profile add/update to immediately update in-memory state.
- Files changed:
  - `context/dataManagement.ts`
  - `context/AppContext.tsx`
  - `components/ProfileManager.tsx`

## Commits Already On `origin/main`
- `18df7d0` `fix(deploy): pass Supabase Vite env vars into Cloud Build Docker build`
- `dea9cea` `fix(auth): require fixed Supabase OAuth redirect URL env var`
- `2d0525c` `feat(profiles): add delete action and fix cloud profile state updates`

## Data Recovery Actions Performed
- Confirmed export files exist in repo root, including:
  - `family_export.two-profiles.cleaned.json`
  - `family_export.karim-only.json`
  - `family_export.cadel-only.json`
- Re-ran import:
  - `npm run migrate:supabase -- --in ./family_export.two-profiles.cleaned.json --owner-id <auth_uid> --dry-run`
  - `npm run migrate:supabase -- --in ./family_export.two-profiles.cleaned.json --owner-id <auth_uid>`
- Last reported import output:
  - `app_users: 2`
  - `coasters: 110`
  - `credits: 162`
  - `wishlist: 3`
  - `Supabase import complete.`

## Remaining Problem
- User sees app working but only ~70+ credits visible (expected more).
- This likely indicates either:
  1. Per-profile split (Karim vs Cadel) where current active profile is not the one with expected total.
  2. ID-level mismatch between imported `credits.user_id` and currently visible profile ids.
  3. Export source (`two-profiles.cleaned`) already reduced relative to an older fuller export.

## High-Priority Next Steps (for next agent)
1. Verify counts in Supabase for the logged-in owner id:
   - `select id, name, owner_id from app_users where owner_id = '<auth_uid>';`
   - `select user_id, count(*) from credits where owner_id = '<auth_uid>' group by user_id order by count(*) desc;`
2. Map `user_id` counts to profile names:
   - `select u.id, u.name, count(c.id) from app_users u left join credits c on c.user_id=u.id and c.owner_id=u.owner_id where u.owner_id='<auth_uid>' group by u.id,u.name order by count(c.id) desc;`
3. Compare DB totals against source file counts to determine if loss happened pre-import:
   - Parse `family_export.two-profiles.cleaned.json` and report per-user credit totals.
4. If Karim expected count is higher than current source file contains, restore from fuller source file (`family_export.json` or profile-specific JSON), then re-import with same `--owner-id`.

## Important Local Git State
- `main` is synced with `origin/main`.
- Uncommitted local modification exists:
  - `scripts/importToSupabase.mjs`
- Why it matters:
  - This local patch makes `--owner-id` forcibly override row-level `ownerId` from source JSON, preventing stale-owner imports.
- Also untracked docs file:
  - `HANDOFF_2026-04-17.md`

## Suggested Immediate Action Tomorrow
- First run the SQL mapping query above and confirm exact credit count per profile id/name for current `auth_uid`.
- If DB has full expected credits but app still undercounts, inspect front-end filtering by `activeUser.id` and initial active user selection logic.
