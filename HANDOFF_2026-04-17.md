# Coaster Credits Handoff (2026-04-17)

## Session Summary
- User reported duplicate-looking entries in Add Credit list and a smaller-than-expected coaster catalog.
- We confirmed true DB duplicates were not present for exact `name+park+country` groups.
- We imported a large external coaster catalog (RobMulla/Kaggle dataset) and fixed cloud load logic so imported coasters actually appear in-app.
- User confirmed final outcome: large coaster list is visible.

## Key Outcomes Completed
- Add Credit UI dedupe behavior implemented to avoid duplicate display rows for same coaster identity.
- New Supabase dedupe tool added for exact duplicate coaster groups:
  - `scripts/dedupeCoastersSupabase.mjs`
  - npm script: `dedupe:coasters:supabase`
- New CSV catalog importer added:
  - `scripts/importCatalogCsv.mjs`
  - npm script: `import:coasters:csv`
- README updated with CSV import flow and Supabase commands.
- Critical cloud load fix shipped:
  - `context/appLifecycle.ts` now includes full cloud coaster catalog (not just `isCustom` subset).

## Cloud/Data Actions Executed This Session
- User ran catalog import with apply mode:
  - `npm run import:coasters:csv -- --in ./coaster_db.csv --apply`
- Import output reported:
  - `CSV rows parsed: 1087`
  - `Unique coaster identities from CSV: 990`
  - `Apply complete. Upserted 990 coaster rows.`
- User ran dedupe script in both dry-run and apply mode:
  - Both reported `No duplicate coaster groups found.`

## Most Important Bug Fixed
- **Bug**: Cloud initialization was setting coasters from `INITIAL_COASTERS + loaded.coasters.filter(isCustom)`.
- **Impact**: Imported global catalog rows (`is_custom=false`) were hidden in UI.
- **Fix**: Merge all cloud coasters + initial coasters by id in `context/appLifecycle.ts`.

## Current Git State
- Branch: `main` ahead of `origin/main` by 3 commits.
- Latest commit:
  - `68d9ccd feat: import large coaster catalog and fix cloud coaster loading`
- Current uncommitted changes/files:
  - Modified: `context/domainActions.ts`
  - Deleted: `firebase.ts`
  - Untracked: `coaster_db.csv`
  - Untracked: `coasters.import.preview.json`
  - Untracked: `scripts/dedupeCoastersSupabase.mjs`

## Notes For Next Agent
1. Decide whether to commit `scripts/dedupeCoastersSupabase.mjs` (currently untracked but useful operational tool).
2. Keep `coaster_db.csv` and `coasters.import.preview.json` out of git unless explicitly desired (large/generated data artifacts).
3. Review `context/domainActions.ts` + deleted `firebase.ts` changes before next release/push (they pre-existed in working tree and were not finalized in this session).
4. If user wants stricter near-duplicate cleanup (not exact match), add a fuzzy merge script for coaster identities (e.g., normalized punctuation/alias matching).

## Validation Performed
- `npm run lint` passed.
- `npm test` passed (7/7 tests).
- Manual confirmation from user: large Add Credit list now visible.

## Useful Commands
- Dev server:
  - `npm run dev`
- CSV import (dry run):
  - `npm run import:coasters:csv -- --in ./coaster_db.csv --out-json ./coasters.import.preview.json`
- CSV import (apply):
  - `npm run import:coasters:csv -- --in ./coaster_db.csv --apply`
- Exact duplicate scan/merge for coasters:
  - `npm run dedupe:coasters:supabase`
  - `npm run dedupe:coasters:supabase -- --apply`
