# Coaster Credits Handoff (2026-04-13)

## Session Summary
- Deployment pipeline was verified and working end-to-end.
- Multiple hardening fixes were implemented for Cloud Build/Cloud Run and sync reliability.
- Data cleanup tooling and per-profile export split files were prepared for migration/recovery.
- Main blocker now is Firestore quota exhaustion on the current free-tier database.

## Key Outcomes Completed
- Cloud deploy verified live (Cloud Build success + Cloud Run URL returning HTTP 200).
- Security hardening completed:
- Removed build-time `GEMINI_API_KEY` injection from Docker image build path.
- Added deployment security note to README.
- JSON import hardening completed:
- Added safer import logic to avoid permission failures from ID collisions and invalid URLs.
- Per-entry photo tooling completed:
- Added "Fetch Web Photo For This Entry" behavior in ride detail/edit flows.
- Improved Wikipedia image fetch fallback strategy for better hit rate.
- Sync resilience improvements completed:
- Added safer sync error handling.
- Added migration sanitization/chunking and safer ID handling in sync migration path.

## Important Runtime/Prod Issue
- Firestore console reported:
- Quota exceeded for free daily read units.
- Database cannot exceed free quota even with billing enabled for this specific free-tier DB.
- User symptoms observed:
- "Cloud sync encountered an issue..."
- "Last cloud sync never"
- Empty profile/credits view despite existing data in exports.

## Data Files Created During Recovery
- Raw export placed in project root:
- `family_export.json`
- Cleaned files generated:
- `family_export.cleaned.json` (single-profile cleanup output)
- `family_export.cleaned.duplicates-report.json`
- `family_export.cleaned.deduped.json` (dedupe run output)
- `family_export.cleaned.deduped.duplicates-report.json`
- Profile-safe file created (keep only Karim + Cadel, remove empty Test):
- `family_export.two-profiles.cleaned.json`
- Per-person split files created for separate account imports:
- `family_export.karim-only.json` (129 credits, 14 wishlist)
- `family_export.cadel-only.json` (114 credits, 0 wishlist)

## Profile Mapping Confirmed
- `u1` -> `Karim`
- `u_mihzrqzy_wjurihrkb` -> `Cadel` (renamed from Rhi)
- `u_mk0gilf8_vzxa2rx4i` -> `Test` (empty)

## Repo Changes in Working Tree (Not Fully Committed in this session)
- Modified:
- `package.json` (added `cleanup:json` script)
- Added:
- `scripts/cleanupExport.mjs`
- Added local JSON artifacts (personal data files listed above).

## Sensitive Data Handling Notes
- Do **not** commit personal export JSON files to git.
- Prefer adding these patterns to `.gitignore` before any commit:
- `family_export*.json`
- `*.duplicates-report.json`

## Recommended Next-Agent Plan
1. Decide backend direction with user:
- Option A: stay on Firebase (blocked by quota/reset windows).
- Option B (recommended): migrate to Supabase with cleanup-aware import.

2. If staying on Firebase short-term:
- Wait for read quota reset.
- Import only per-person files into matching Google accounts:
- Karim imports `family_export.karim-only.json`.
- Cadel imports `family_export.cadel-only.json`.

3. If migrating to Supabase:
- Create schema for `users`, `coasters`, `credits`, `wishlist`.
- Add import script to ingest per-person JSON safely.
- Enforce unique constraints to prevent duplicate coaster proliferation.
- Move auth + sync reads/writes from Firebase to Supabase incrementally.

4. Cleanup tooling follow-up:
- Current `scripts/cleanupExport.mjs` defaults to single keeper profile.
- Add a new mode to preserve multiple selected profiles (e.g. `--keep-users u1,u_xyz`), to avoid ad-hoc inline node scripts.

## Useful Commands
- Cleanup script:
- `npm run cleanup:json -- --in ./family_export.json --keep-user-id "u_mihzrqzy_wjurihrkb"`
- Deploy:
- `gcloud builds submit --config cloudbuild.yaml .`
- Quick status:
- `git status --short --branch`

## User Intent at End of Session
- User paused for later.
- Requested a handoff so next agent can continue without losing context.
- User wants data-safe continuation and eventually separate account ownership (Karim/Cadel).
